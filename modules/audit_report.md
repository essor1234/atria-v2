# Codebase Audit Report - Atria Modules

**Date:** 2026-07-01  
**Scope:** /app/modules (batch_reports, item_flow_tracking, warehouse)

---

## 1. TODO/FIXME Comments

**Status:** Clean - No TODO/FIXME comments found in any module.

All codebases are free of pending technical debt markers.

---

## 2. Functions Exceeding 80 Lines

**Status:** Clean - No single function exceeds 80 lines.

| File | Total Lines | Largest Function |
|------|-------------|------------------|
| batch_reports/scripts/report.py | 208 | `cmd_gen` (~40 lines) |
| item_flow_tracking/scripts/flow.py | 978 | `cmd_order_new` (~80 lines) |
| item_flow_tracking/scripts/_db.py | 288 | `connect()` (~15 lines) |
| warehouse/scripts/inventory.py | 597 | `cmd_state_list` (~60 lines) |
| warehouse/scripts/_db.py | 178 | `connect()` (~15 lines) |
| warehouse/scripts/push_form.py | 106 | `main()` (~60 lines) |

**Note:** While `flow.py` is 978 lines total, it's well-organized with many small focused functions. Consider splitting into submodules if maintainability becomes an issue.

---

## 3. Unlogged `except Exception` Blocks

**Status:** 2 instances found - requires attention.

| File | Line | Context |
|------|------|---------|
| batch_reports/scripts/report.py:75 | `except Exception:` | Skip unreadable/partial files in `_load_reports()` |
| batch_reports/scripts/report.py:133 | `except Exception:` | In `cmd_merge()` error handling |

**Recommendation:** Both instances have `# noqa: BLE001` comments indicating intentional broad exception handling for specific use cases (file resilience). Consider adding explicit logging or at minimum documenting why broad catching is necessary.

---

## 4. Async Functions Without Return Type Hints

**Status:** Clean - No async functions found in the codebase.

All modules use synchronous Python patterns. The codebase is not using async/await at all.

---

## 5. Circular Import Risks

**Status:** Clean - No circular imports detected.

Import patterns observed:
- `flow.py` imports `_db` (local sibling module)
- `inventory.py` imports `_db` (local sibling module)  
- `push_form.py` imports `_db` (local sibling module)

All imports follow a clear dependency direction with no cycles.

---

## Summary

| Category | Status | Count |
|----------|--------|-------|
| TODO/FIXME | ✅ Clean | 0 |
| Long Functions (>80 lines) | ✅ Clean | 0 |
| Unlogged Exceptions | ⚠️ Found | 2 |
| Async Without Hints | ✅ Clean | 0 |
| Circular Imports | ✅ Clean | 0 |

**Overall Health:** Good - The codebase follows solid Python practices with minimal issues. The only concern is the 2 unlogged exception blocks which are documented but could benefit from explicit logging for better observability.

**Recommendations:**
1. Add logging to the 2 exception handlers in `report.py`
2. Consider refactoring `flow.py` (978 lines) into submodules for better organization
3. Continue maintaining the clean import structure

---

*Report generated by automated code audit*